# 本地开发环境配置
# 复制此文件为 .env.local 并根据需要修改

# 网站配置
NEXT_PUBLIC_SITE_URL=http://localhost:3000
NEXT_PUBLIC_SITE_NAME="JKAnime FLV"

# ========================================
# API 配置选项 (选择其中一种)
# ========================================

# 选项 1: 使用本地 API 服务器 (需要启动本地 API)
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080
USE_MOCK_API=false

# 选项 2: 使用生产 API (推荐，无需本地 API)
# NEXT_PUBLIC_API_BASE_URL=https://api-jk.funnyu.xyz
# USE_MOCK_API=false

# 选项 3: 使用 Mock API (离线开发)
# 注释掉 NEXT_PUBLIC_API_BASE_URL 并设置:
# USE_MOCK_API=true

# 其他配置
NODE_ENV=development
NEXT_TELEMETRY_DISABLED=1

# 注意: 如果你没有本地 API 服务器，建议使用选项 2 (生产 API)